packages:
  - "frontend"

# postcss <8.5.10 bundled by next/autoprefixer is vulnerable to XSS via
# unescaped </style> in stringified output (GHSA-qx2v-qp2m-jg93). Force
# every resolution to the patched line.
overrides:
  postcss: ^8.5.10
